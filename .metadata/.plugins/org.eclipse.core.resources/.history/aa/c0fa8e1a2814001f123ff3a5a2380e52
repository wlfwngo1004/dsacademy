#myForm {
    position: fixed;
    top: 50%;
    left: 50%;
    transform: translate(-50%, -50%);
    width: 400px; /* 원하는 폭으로 조정 */
    height: 200px; /* 원하는 높이로 조정 */
    text-align: center; /* 텍스트를 가운데 정렬합니다. */
}

#myForm div {
    margin-top: 20px; /* input과 button 위쪽에 여백을 추가합니다. */
}

#m_atho_num, #loginBtn {
    margin-bottom: 10px; /* input과 button 아래쪽에 여백을 추가합니다. */
}